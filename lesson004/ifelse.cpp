/*
    Операторы ветвления
    if - если
    else - иначе
    if (логическое выражение) {
        //Выполняются если вырожение true  
    } else {
        //Выполняется если выражение false
    }
*/

#include <iostream>

using namespace std;

int main() 
{
    /*int age;
    cout << "Please enter your age: ";
    cin >> age;

    if (age < 18)
    {
        cout << "You too young";
        return -1; //Access denied
    }

    cout << "Accepted";*/


    /*int balance = 200;
    int price;
    cout << "Enter item price: ";
    cin >> price;

    if (balance < price) {
        cout << "You cannot buy it" << endl;
    } else {
        cout << "You can buy it, and your charge : " << balance - price << endl;
    }

    cout << "Exit";*/

    /*int weight;
    cout << "Enter your weight(kg): ";
    cin >> weight;

    if (weight < 0) {
        cout << "Incorrect!";
        return -1; // Incorrect input
    } else if (weight < 20) {
        cout << "You baby";
    } else if (weight <= 50) {
        cout << "You young";
    } else if (weight < 100) {
        cout << "You old";
    } else {
        cout << "You fat";
    }*/

    /*bool a = false;
    
    if (a) {
        cout << "A";
        cout << "C";    
    }

     if (a)
        cout << "A";
        cout << "C";*/

    return 0;
}